
// const protocol = "";

interface Url {
  [propertys: string]: string[];
}

const host = "";
const url = {
  user: {
    login: 'user/login'
  }
}

// function addHost(): string[] {
//   let urls: string[] = [];
//   for (let i in url) {
//     url[i].map((value) => {
//       urls.push(`${url[i]}/${value}`);
//     });
//   }
//   return urls;
// }

export default {
  host, url
};
